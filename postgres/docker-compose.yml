# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres:12.7
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: nimda
      POSTGRES_USER: admin
